app = "hermes-gateway"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile.hermes"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true